name: Release Mod

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5          # 升级至 v5

      - name: Set up JDK 21
        uses: actions/setup-java@v5        # 升级至 v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4   # 升级至 v4
        with:
          cache-disabled: true                 # 禁用缓存，解决 400 错误

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Upload JAR to Release
        uses: softprops/action-gh-release@v3   # 升级至 v3
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: build/libs/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
